export interface Plano {
  id: string;
  nome: string;
  valor: number;
  descricao: string;
  ordem: number;
}

export interface PlanoResponse {
  id: string;
  name: string;
  price: string;
  description: string;
  order: number;
}